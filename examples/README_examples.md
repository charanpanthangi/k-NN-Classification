# Examples

This folder can host lightweight demos (scripts, notebooks, plots) that showcase how to tweak hyperparameters or swap datasets. For now, use `app/main.py` or the notebook in `notebooks/demo_knn_classification.ipynb` as the primary examples.
